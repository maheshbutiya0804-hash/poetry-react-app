# Admin Requests reference UI

Updated Admin → Requests to match the supplied Heartstring main layout while preserving Laurentine's live request API, status updates, search/filtering, responsive layouts, and 10-record pagination. Added a non-destructive request detail modal in place of the previous alert-based View action.
